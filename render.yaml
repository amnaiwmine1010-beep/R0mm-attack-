services:
  - type: web
    name: nirob-app
    env: python
    region: ohio
    plan: free
    buildCommand: |
      python3.11 -m pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python app.py
